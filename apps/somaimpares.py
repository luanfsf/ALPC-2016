#!/usr/bin/env python
print "digite ate que valor deseja saber a soma dos numeros impares"
ate = int(input())
conta = 0 
while conta  < ate:
  conta = conta +1
  if (conta % 2 == 0): #even 
    
  else: #odd